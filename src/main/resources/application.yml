spring:
  application:
    name: ForgeIQ

jwt:
  secret: ${JWT_SECRET}
  access-expiration: 86400000
  refresh-expiration: 604800000

gemini:
  api-key: ${GEMINI_API_KEY}
  model: gemini-2.5-flash

logging:
  level:
    root: INFO